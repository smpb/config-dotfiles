# reload tmux.conf with prefix-r
bind r source-file ~/.tmux.conf \; display 'Reloaded config file'

set -g default-terminal 'screen-256color'
set -g focus-events     on  # focus events are requested and passed to applications

#
# COLORS
#

set -g status-style               "fg=white,dim bg=default"

set  -g message-style             "fg=white bg=black,bright"

set -g pane-border-style          "fg=white,dim bg=default"
set -g pane-active-border-style   "fg=blue,dim bg=default"

setw -g window-status-bell-style  "fg=white bg=red,bold"

#
# STATUS BAR
#

# Variable           Description
# --------           -----------
# #H                 Hostname of local host
# #h                 Hostname of local host without the domain name
# #F                 Current window flag
# #I                 Current window index
# #P                 Current pane index
# #S                 Current session name
# #T                 Current window title
# #W                 Current window name
# ##                 A literal #

status_cpu="#(cut -d ' ' -f 1-3 /proc/loadav)"
if-shell '[[ $(uname -s) = Darwin ]]' {
  status_cpu="#(uptime | cut -d ',' -f 4- | cut -d ':' -f 2 | awk '{$1=$1};1')"
}

setw -g window-status-format          " #[fg=white,dim]#F#I #W "
setw -g window-status-current-format  " #[fg=white,bold bg=blue]#F#I #W "

set -g status-left-length 10
set -g status-left        "#[default][ #[fg=red,dim]#S #[default]] "

set -g status-right-length  55
set -g status-right         "[ #[fg=green]$status_cpu @ #h#[default] | #[fg=magenta]%a %d/%m/%Y %H:%M#[default] ]"

#
# WINDOWING
#

# bind | and - to horizontal and vertical split respectively
bind | split-window -h
bind - split-window -v

# provide visual notification in status bar when a window's content changes
setw -g monitor-activity on
set  -g visual-activity  on

#
# MOUSE MODE
#

setw -g mouse on
